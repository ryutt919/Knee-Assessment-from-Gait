name: scalar_clean_multispeed
pipeline_version: "3.1"
role: primary
aggregation: cycle_mean_then_equal_trial_mean
unit: session
feature_count: 405
blocks:
  scalar:
    speeds: [slow, normal, fast]
    sides: [injured, contralateral]
    channels: 9
    metrics: [peak, min, ROM, IC_angle, peak_timing]
    symmetry: bounded_signed_difference
